#include <stdio.h>
#include <stdlib.h> 
#include <string.h>

int main(argc, argv[]) {
	char[100] input;
	int bill_id; // Unique Id starting at #300.
	char* Client_Id;
	
	printf("Enter command: \n");
	printf("Type 'Quit' to exit\n");

	while(strcmp(input, "Quit")!=0){
		printf("Enter command: \n");
		fgets(input, sizeof(input), stdin);
		
		if (strcmp(input, "Process"!=0){
			process_func(); // fetches date implicitly
		}

		if (strmp(input, "log")!=0){
			printf("Client name: \n");
			fgets(input, sizeof(input), stdin);

			if (ClientInDatabase(client)!=1){
				printf("Client is not in database. Creating a new client.");
				printf	

				
				
				
					
			
		
		
		
			
